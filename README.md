# Mount & Blade Discord Bot

Railway Variables:
- DISCORD_TOKEN = Bot Token
- CLIENT_ID = Application ID
- GUILD_ID = Server ID

Never put the token in GitHub.

After deployment use /setup. Commands: /rules /server /setup /clear /kick /ban /clan.
